export function SettingsPage() {
  return (
    <section className="ad-page">
      <h1>Settings</h1>
      <p className="lede">Account, API keys (client hints only), preferences.</p>
    </section>
  );
}
